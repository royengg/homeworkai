# Non-secret values used only by compose.test.yaml.
DATABASE_URL=postgresql://homeworkai:homeworkai-test-password@postgres:5432/homeworkai
JWT_SECRET=test-only-jwt-secret-at-least-32-characters
ALLOWED_ORIGINS=http://localhost:8080
GOOGLE_API_KEY=test-only-google-key
STORAGE_ENDPOINT=https://storage.example.invalid
STORAGE_REGION=auto
STORAGE_ACCESS_KEY_ID=test-only-access-key
STORAGE_SECRET_ACCESS_KEY=test-only-secret-key
STORAGE_BUCKET=test-only-bucket
MAX_FILE_SIZE_MB=10
FRONTEND_PORT=8080

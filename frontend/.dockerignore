# Don't ship host artifacts into the image — keeps build context small and
# prevents leaking local .env, node_modules, or build outputs.
node_modules
dist
.env
.env.*
!.env.example
.git
.gitignore
.vscode
*.log
logs
README.md